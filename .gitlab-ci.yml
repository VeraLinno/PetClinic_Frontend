stages:
  - verify
  - package
  - deploy

default:
  tags:
    - petclinic-runner

variables:
  GIT_STRATEGY: none

before_script:
  - set -e
  - export WORKDIR="$(mktemp -d -p /tmp petclinic-frontend-XXXXXX)"
  - git clone --branch "$CI_COMMIT_REF_NAME" --depth 1 "$CI_REPOSITORY_URL" "$WORKDIR"
  - cd "$WORKDIR"

frontend_verify:
  stage: verify
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
    - if: '$CI_COMMIT_BRANCH == "main"'
    - if: '$CI_COMMIT_BRANCH == "develop"'
  script:
    - docker info
    - >
      docker run --rm
      -v "$WORKDIR":/src
      -w /src
      node:20-alpine
      sh -lc "npm ci && npm run build && npm run test"

frontend_container_smoke:
  stage: package
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
    - if: '$CI_COMMIT_BRANCH == "main"'
    - if: '$CI_COMMIT_BRANCH == "develop"'
  needs:
    - job: frontend_verify
  script:
    - docker info
    - docker build -t petclinic-frontend:$CI_COMMIT_SHORT_SHA "$WORKDIR"
    - docker rm -f petclinic-frontend-smoke || true
    - docker run -d --name petclinic-frontend-smoke -p 18081:80 petclinic-frontend:$CI_COMMIT_SHORT_SHA
    - |
      for i in $(seq 1 45); do
        if docker run --rm --network container:petclinic-frontend-smoke curlimages/curl:8.8.0 -fsS http://localhost/ >/dev/null 2>&1; then
          echo "Frontend container smoke check passed"
          docker rm -f petclinic-frontend-smoke >/dev/null 2>&1 || true
          exit 0
        fi
        sleep 2
      done
      echo "ERROR: Frontend container smoke check failed"
      docker logs petclinic-frontend-smoke --tail 200 || true
      docker rm -f petclinic-frontend-smoke >/dev/null 2>&1 || true
      exit 1

deploy_frontend:
  stage: deploy
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
  needs:
    - job: frontend_verify
    - job: frontend_container_smoke
  script:
    - docker info
    - docker network inspect petclinic_default >/dev/null 2>&1 || docker network create petclinic_default
    - docker build -t petclinic-frontend:latest "$WORKDIR"
    - docker rm -f petclinic-frontend || true
    - docker run -d --name petclinic-frontend --restart unless-stopped --network petclinic_default -p 80:80 petclinic-frontend:latest
    - docker ps --filter name=petclinic-frontend
    - |
      wait_for_url() {
        local url="$1"
        local name="$2"
        local retries=30
        local delay=2

        for i in $(seq 1 $retries); do
          if command -v curl >/dev/null 2>&1; then
            if curl -fsS "$url" >/dev/null; then
              echo "$name is healthy: $url"
              return 0
            fi
          else
            if wget -qO- "$url" >/dev/null; then
              echo "$name is healthy: $url"
              return 0
            fi
          fi
          echo "Waiting for $name ($i/$retries): $url"
          sleep "$delay"
        done

        echo "ERROR: $name failed health check: $url"
        docker logs petclinic-frontend --tail 200 || true
        return 1
      }

      wait_for_url "http://localhost/" "frontend"
      wait_for_url "http://localhost/api/v1/health" "frontend->api proxy"
